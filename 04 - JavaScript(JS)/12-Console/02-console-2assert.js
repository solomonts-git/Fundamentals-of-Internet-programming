// The assert() method writes a message to the console if an expression evaluates to false.
// Syntax
// console.assert(expression, message)

let x = 4;
let y = 3;
console.assert(x + y == 11, "assertion did not work");
console.assert(x + y == 7, "assertion work no message to console");
